export enum AppRoutes {
  HOME = "/",
  ABOUT = "/about",
  CONTACT = "/contact",
  PRODUCTS = "/products",
  PRODUCT = "/products/:id",
  LOGIN = "/login",
  REGISTER = "/register",
  CART = "/cart",
  WISH_LIST = "/wishlist",
  CHECKOUT = "/checkout",
}
